# Static site — the dashboard is a frozen retrospective (job search concluded
# 2026-07-24). All data is embedded in index.html; there is no serverless
# function or API redirect anymore. Nothing to build, just publish the files.
[build]
  publish = "."
